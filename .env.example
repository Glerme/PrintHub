# Copie este arquivo para .env e preencha os valores
# .env NÃO deve ser commitado

# Variáveis expostas ao frontend (prefixo VITE_ obrigatório)
# VITE_EXEMPLO=valor

# Variáveis acessíveis apenas no backend Rust via std::env::var()
# EXEMPLO_BACKEND=valor
