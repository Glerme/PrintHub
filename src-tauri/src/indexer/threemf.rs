// 3MF parser: extracts embedded thumbnail + print metadata.
// 3MF is a ZIP archive containing XML files + optional Thumbnails/thumbnail.png.
// Uses the `zip` and `quick-xml` crates. Runs in spawn_blocking.
